version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: postgres_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password-01
      POSTGRES_DB: movie_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
  app:
    build: .
    container_name: moviehub_app
    depends_on:
      - postgres
    ports:
      - "8080:8080"
    environment:
      DB_URL: postgres://postgres:password-01@postgres:5432/movie_db?sslmode=disable
      GOOSE_DRIVER: postgres
      GOOSE_DBSTRING: postgres://postgres:password-01@postgres:5432/movie_db?sslmode=disable
      GOOSE_MIGRATION_DIR: /moviehub/internal/storage/postgre/migrations
  #mongodb:
  #  image: mongo:7
  #  container_name: mongodb
  #  restart: always
  # ports:
  #    - "27017:27017"
  #  volumes:
  #    - mongo_data:/data/db
    #  - ./init-mongo.js:/docker-entrypoint-initdb.d/init-mongo.js:ro
      

  redis:
    image: redis:8.4
    container_name: redis
    restart: unless-stopped
    ports:
      - "6379:6379"

volumes:
  postgres_data:
  #mongo_data:
